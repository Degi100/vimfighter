import Nav from "../navbar/Nav";
import '../../styles/header.scss';

const Header = () => {
  return (
    <div className="header">
      <Nav />
    </div>
  );
};

export default Header;
